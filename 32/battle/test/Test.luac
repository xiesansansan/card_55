require("server/depend")

AttrHelper = {
	parseDmageRatioValue = function (slot0)
		slot1 = {}
		slot2 = string.split(slot0, ",")

		for slot6 = 1, 2, 1 do
			slot1[slot6] = tonumber(slot2[slot6]) or 0
		end

		return slot1
	end,
	parseStaticConditionMap = function (slot0)
		slot1 = {}

		for slot6, slot7 in ipairs(slot2) do
			if tonumber(slot7) then
				slot1[slot8] = true
			end
		end

		return slot1
	end
}

return 
